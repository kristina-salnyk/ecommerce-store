import styled from 'styled-components/native';

export const AvatarModalActionsStyled = styled.View`
  margin-top: ${({theme}) => theme.space.x16};
  flex-direction: row;
  gap: ${({theme}) => theme.space.x32};
`;
