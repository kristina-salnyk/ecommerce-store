import {StyleSheet} from 'react-native';

export const ImageSliderStyle = StyleSheet.create({
  container: {
    marginTop: -15,
  },
  dot: {
    width: 10,
    height: 10,
    borderRadius: 5,
    marginHorizontal: 0,
  },
});
